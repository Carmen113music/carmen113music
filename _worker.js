export default { async fetch(request, env) { const url = new URL(request.url); const pathname = url.pathname; if (pathname === '/resolve-maps') { const mapsUrl = url.searchParams.get('url'); if (!mapsUrl) return new Response(JSON.stringify({ error: 'missing url' }), { status: 400, headers: { 'Content-Type': 'application/json' } }); try { const r = await fetch(mapsUrl, { redirect: 'follow' }); const final = r.url; const m = final.match(/@(-?[\d.]+),(-?[\d.]+)/); const q = new URL(final).searchParams.get('q'); return new Response(JSON.stringify({ resolved: final, lat: m?.[1], lng: m?.[2], q }), { headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*' } }); } catch (e) { return new Response(JSON.stringify({ error: e.message }), { status: 500, headers: { 'Content-Type': 'application/json' } }); } } return env.ASSETS.fetch(request); } };
